import { Text, View } from "react-native";

export const SearchChatScreen = ({navigation, route}) => {
    return (
        <View style={{justifyContent: "center", alignItems: "center", flexDirection: "column", height: "100%"}}>
            <Text>This is Search Chat screen</Text>
        </View>
    );
}